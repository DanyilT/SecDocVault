/**
 * app/controllers/index.ts
 *
 * Controller entrypoints for the application. Re-exports hooks and controller
 * factories used by the app bootstrap.
 */

export { useAppController } from './useAppController';
